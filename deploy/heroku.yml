build:
  docker:
    web: docker/Dockerfile
    worker: docker/Dockerfile.worker

run:
  web: python -m src.bot.main
  worker:
    command:
      - celery
      - -A
      - src.workers.celery_app
      - worker
      - --loglevel=info
    scale: 2
